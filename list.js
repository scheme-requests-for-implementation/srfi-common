var options = {
  valueNames: [ "authors",
                "date",
                "name",
                "number",
                { name: "status", attr: "data-status" }
              ]};

var srfiList = new List("srfis", options);